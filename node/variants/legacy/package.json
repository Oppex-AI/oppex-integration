{
  "name": "@oppex/integration-sdk-legacy",
  "version": "1.0.0",
  "description": "Node.js SDK for posting incidents to Oppex (Node >=8, http/https transport). For Node >=18, use @oppex/integration-sdk instead.",
  "license": "Apache-2.0",
  "homepage": "https://oppex.ai",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "smoke": "node test/smoke.js",
    "build:all": "scripts/build-all.sh",
    "sanity": "scripts/docker-sanity.sh",
    "regression": "tsc -p test/tsconfig.json && node test/regressiontest.js"
  },
  "devDependencies": {
    "@types/node": "12.20.55",
    "typescript": "^5.5.4",
    "winston": "^3.14.2"
  }
}
