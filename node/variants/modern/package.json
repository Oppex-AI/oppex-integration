{
  "name": "@oppex/integration-sdk",
  "version": "1.0.0",
  "description": "Node.js SDK for posting incidents to Oppex (Node >=18, native fetch transport)",
  "license": "Apache-2.0",
  "homepage": "https://oppex.ai",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "smoke": "node test/smoke.js",
    "build:all": "scripts/build-all.sh",
    "sanity": "scripts/docker-sanity.sh",
    "regression": "tsc -p test/tsconfig.json && node test/regressiontest.js"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^18.19.50",
    "winston": "^3.14.2"
  }
}
