"""Tests for the Oppex Python SDK.

Every test runs unchanged on Python 2.7 and Python 3, uses only the standard
library, and never reaches the network beyond loopback.
"""

from __future__ import absolute_import
