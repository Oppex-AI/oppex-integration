// The SDK version. CMake reads this file so the project version and the version
// the library reports can never drift apart. Not public API.

#ifndef OPPEX_SRC_VERSION_HPP
#define OPPEX_SRC_VERSION_HPP

#define OPPEX_SDK_VERSION "1.0.0"

#endif  // OPPEX_SRC_VERSION_HPP
